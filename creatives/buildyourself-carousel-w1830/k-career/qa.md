# QA — `k-career` ("Pehle apna career.")

**Verdict: `pass`.** Full rationale, per-slide sourcing, the negative-list/POV
checklist, both reviewers, and every recorded deviation live one level up in
[`../qa.md`](../qa.md) — that file covers all 13 ads in this set as one QA pass,
since they were built and reviewed together. This stub exists only because
`ad-agent snap-push` resolves QA at `<creative_ref>/qa.md`, one path per ad; it
is not a separate review.

Sree (app owner) — `pass`, 2026-08-30, verbal, on the full set.
Building session — self re-check, `pass`, 2026-08-30, on the full set.
